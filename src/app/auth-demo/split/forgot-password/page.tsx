import { SplitForgotPasswordView } from 'src/sections/auth-demo/split';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Auth Split: Forgot Password',
};

export default function Page() {
  return <SplitForgotPasswordView />;
}
