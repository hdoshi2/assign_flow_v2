import { SplitRegisterView } from 'src/sections/auth-demo/split';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Auth Split: Register',
};

export default function Page() {
  return <SplitRegisterView />;
}
