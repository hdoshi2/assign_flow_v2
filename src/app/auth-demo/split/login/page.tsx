import { SplitLoginView } from 'src/sections/auth-demo/split';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Auth Split: Login',
};

export default function Page() {
  return <SplitLoginView />;
}
