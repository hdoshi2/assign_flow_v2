import { SplitVerifyView } from 'src/sections/auth-demo/split';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Auth Split: Verify',
};

export default function Page() {
  return <SplitVerifyView />;
}
