import { SplitNewPasswordView } from 'src/sections/auth-demo/split';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Auth Split: New Password',
};

export default function Page() {
  return <SplitNewPasswordView />;
}
