import { CenteredNewPasswordView } from 'src/sections/auth-demo/centered';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Auth Centered: New Password',
};

export default function Page() {
  return <CenteredNewPasswordView />;
}
