import { CenteredRegisterView } from 'src/sections/auth-demo/centered';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Auth Centered: Register',
};

export default function Page() {
  return <CenteredRegisterView />;
}
