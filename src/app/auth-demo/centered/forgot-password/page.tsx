import { CenteredForgotPasswordView } from 'src/sections/auth-demo/centered';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Auth Centered: Forgot Password',
};

export default function Page() {
  return <CenteredForgotPasswordView />;
}
